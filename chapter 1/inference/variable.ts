let numeric = 5;        // type inference -typescript guess the type is number.

let fname;
fname = "Mike";         // type inference don't work when declaration and assignment occurs in different step.


